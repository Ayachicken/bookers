<p><strong>Editing Book</strong></p>

<%= form_for(@book) do |f| %>
	<p>Title</p>
	<%= f.text_field :title %>
	<p>Body</p>
	<%= f.text_field :body %>
	<br>
	<%= f.submit 'Update Book' %>
<% end %>
<p>
<%= link_to 'show', book_path(@book) %>|
<%= link_to 'Back', books_path %>
</p>